import ProductForm from '@/components/ProductForm';import {prisma} from '@/lib/prisma';import {notFound} from 'next/navigation';export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=await prisma.product.findUnique({where:{id:Number(id)}});if(!p)notFound();return <><div className="adminTop"><div><h1>পণ্য Edit</h1><p>{p.name}</p></div></div><ProductForm product={p}/></>}
