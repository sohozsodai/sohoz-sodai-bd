import { NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const p=await prisma.product.findUnique({where:{id:Number(id)}}); return p?NextResponse.json(p):NextResponse.json({error:'not found'},{status:404})}
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params; const b=await req.json(); const p=await prisma.product.update({where:{id:Number(id)},data:{name:b.name,slug:b.slug||undefined,image:b.image||null,images:JSON.stringify(b.images||[]),category:b.category||'অন্যান্য',subcategory:b.subcategory||null,regularPrice:Number(b.regularPrice||0),salePrice:Number(b.salePrice||b.regularPrice||0),discount:Number(b.discount||0),stock:Number(b.stock||0),sku:b.sku,details:b.details||'',sizes:JSON.stringify(b.sizes||[]),colors:JSON.stringify(b.colors||[]),variants:JSON.stringify(b.variants||[]),active:!!b.active,featured:!!b.featured,offer:!!b.offer}});return NextResponse.json(p)}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;await prisma.product.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}
