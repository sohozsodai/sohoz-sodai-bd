'use client';import {CartProvider,useCart} from '@/components/cart';import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {money} from '@/lib/data';
function Form(){const {items,total}=useCart();const r=useRouter();const [areas,setAreas]=useState<any[]>([]),[settings,setSettings]=useState<any>({});const [f,setF]=useState<any>({name:'',phone:'',email:'',address:'',deliveryAreaId:'',paymentMethod:'COD',paymentRef:'',note:''});const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);useEffect(()=>{Promise.all([fetch('/api/delivery-areas').then(x=>x.json()),fetch('/api/settings').then(x=>x.json())]).then(([a,s])=>{const active=a.filter((x:any)=>x.active);setAreas(active);setSettings(s);if(active[0])setF((v:any)=>({...v,deliveryAreaId:String(active[0].id)}))})},[]);if(!items.length)return <main className="section narrow"><h1>Checkout</h1><p>কার্ট খালি।</p></main>;const area=areas.find(x=>String(x.id)===String(f.deliveryAreaId));const fee=area?Number(area.charge):Number(settings.deliveryCharge||0);async function submit(e:any){e.preventDefault();setLoading(true);setMsg('');const res=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,items})});const data=await res.json();setLoading(false);if(!res.ok)return setMsg(data.error||'অর্ডার হয়নি');localStorage.removeItem('cart');r.push('/order-success?order='+data.orderNo)}return <main className="section narrow"><h1>অর্ডার সম্পন্ন করুন</h1><form className="form" onSubmit={submit}><div className="formGrid"><label>নাম<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>মোবাইল<input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Email (optional)<input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Delivery Area<select required value={f.deliveryAreaId} onChange={e=>setF({...f,deliveryAreaId:e.target.value})}>{areas.map(a=><option value={a.id} key={a.id}>{a.name} — {money(a.charge)}</option>)}</select></label></div><label>Delivery Address<textarea required value={f.address} onChange={e=>setF({...f,address:e.target.value})}/></label><label>Payment Method<select value={f.paymentMethod} onChange={e=>setF({...f,paymentMethod:e.target.value})}><option value="COD">Cash on Delivery</option><option value="bKash">bKash — {settings.bkash}</option><option value="Nagad">Nagad — {settings.nagad}</option></select></label>{f.paymentMethod!=='COD'?<div className="paymentNotice"><b>{f.paymentMethod} Payment</b><p>{f.paymentMethod==='bKash'?settings.bkash:settings.nagad} নম্বরে payment করে Transaction ID দিন।</p><label>Transaction/Reference<input required value={f.paymentRef} onChange={e=>setF({...f,paymentRef:e.target.value})}/></label></div>:null}<label>নোট<textarea value={f.note} onChange={e=>setF({...f,note:e.target.value})}/></label><div className="checkoutSummary"><p><span>পণ্যের মূল্য</span><b>{money(total)}</b></p><p><span>Delivery Charge</span><b>{money(fee)}</b></p><p className="grand"><span>মোট</span><b>{money(total+fee)}</b></p></div>{msg?<div className="error">{msg}</div>:null}<button className="primary full" disabled={loading}>{loading?'অর্ডার হচ্ছে...':'অর্ডার কনফার্ম করুন'}</button></form></main>};export default function Checkout(){return <CartProvider><Form/></CartProvider>}
