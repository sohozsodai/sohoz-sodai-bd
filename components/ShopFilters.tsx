'use client';import {useRouter,useSearchParams} from 'next/navigation';
export default function ShopFilters({categories}:{categories:any[]}){const r=useRouter(),sp=useSearchParams();function go(k:string,v:string){const p=new URLSearchParams(sp.toString());v?p.set(k,v):p.delete(k);r.push('/?'+p.toString()+'#products')}return <div className="filters"><input defaultValue={sp.get('q')||''} placeholder="পণ্যের নাম বা SKU খুঁজুন..." onKeyDown={e=>{if(e.key==='Enter')go('q',(e.target as HTMLInputElement).value)}}/><select value={sp.get('category')||''} onChange={e=>go('category',e.target.value)}><option value="">সব Category</option>{categories.map(c=><option key={c.id}>{c.name}</option>)}</select><select value={sp.get('sort')||'new'} onChange={e=>go('sort',e.target.value)}><option value="new">নতুন আগে</option><option value="low">কম দাম আগে</option><option value="high">বেশি দাম আগে</option></select><button onClick={()=>r.push('/#products')}>Reset</button></div>}
